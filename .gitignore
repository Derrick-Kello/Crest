.derivedData/
.icon-venv/

# Release artefacts from packaging/release.sh
.release/
packaging/release.env

# Separate repo, pushed on its own
packaging/homebrew-tap/

# Release artefacts, built by packaging/ and attached to a GitHub release
# rather than committed.
*.dmg
